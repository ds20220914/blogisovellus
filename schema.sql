CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name TEXT,
    password TEXT,
);


CREATE TABLE School (
    id SERIAL PRIMARY KEY,
    Blog_name TEXT,
    Blog_text TEXT,
);

CREATE TABLE Life (
    id SERIAL PRIMARY KEY,
    Blog_name TEXT,
    Blog_text TEXT,
);


CREATE TABLE Sport (
    id SERIAL PRIMARY KEY,
    Blog_name TEXT,
    Blog_text TEXT,
);

CREATE TABLE Game (
    id SERIAL PRIMARY KEY,
    Blog_name TEXT,
    Blog_text TEXT,
);
